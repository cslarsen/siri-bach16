\usepackage[norsk]{babel}
\usepackage[utf8]{inputenc}

% Kortere marginer
\usepackage[a4paper, margin=2.5cm]{geometry}

% Latin1 Times Roman font for Norwegian
\usepackage{times}
\usepackage[T1]{fontenc}

\usepackage{parskip} % newlines instead of paragraph indentation
\usepackage[nottoc]{tocbibind} % biblio in ToC
\usepackage[hidelinks]{hyperref} % clickable links
\usepackage{apacite} % APA-style bibliography
\usepackage{pdfpages} % to include pdf files

% Fixes accidentally hitting alt+shift+space on OSX.
\DeclareUnicodeCharacter{00A0}{ }

% Tankestrek
\newcommand{\strek}{\textthreequartersemdash}

% 1.5 linjespace
\usepackage{setspace}
\onehalfspacing

% Build index, using the new imakeidx in TeX Live, see
% http://tex.blogoverflow.com/2012/09/dont-forget-to-run-makeindex/
\usepackage{imakeidx}
\makeindex

% Above/below refs
\usepackage{varioref}

% Fjern "Kapittel" i kapitler
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\bfseries}{}{0pt}{\Huge}

\usepackage{tabularx}
\usepackage{graphicx}
\usepackage{pdflscape}
\usepackage{booktabs}
\usepackage{ltablex}

% Quotation, make it smaller for >3 lines
\usepackage[autostyle,threshold=3,thresholdtype=lines]{csquotes}
\newenvironment*{smallquote}
  {\quote\small}
  {\endquote}
\SetBlockEnvironment{smallquote}

% Show fourth level chapters (subsubsection) in toc
\setcounter{tocdepth}{4}
\setcounter{secnumdepth}{4}
